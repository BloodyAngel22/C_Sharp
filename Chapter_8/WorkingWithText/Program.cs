class Program
{
	public static void Main(string[] args)
	{
		// string city = "London";
		// System.Console.WriteLine(format: "City: {0} is length {1}", city, city.Length);

		// string cities = "London, Sydney, New York, Moscow, Tehran";
		// string[] citiesArray = cities.Split(", ");
		// System.Console.WriteLine(format: "There are {0} items in the array", citiesArray.Length);
		// foreach (var item in citiesArray)
		// {
		// 	System.Console.WriteLine(item);	
		// }

		// string fullName = "Alan Jones";
		// int indexOfTheSpace = fullName.IndexOf(" ");
		// string firstName = fullName.Substring(0, indexOfTheSpace);
		// string lastName = fullName.Substring(indexOfTheSpace + 1);
		// System.Console.WriteLine(format: "Full Name: {0}", fullName);
		// System.Console.WriteLine(format: "First Name: {0}, Last Name: {1}", firstName, lastName);

		// string str = "Jones, Alan";
		// int indexOfTheSpace = str.IndexOf(' ');
		// string fullName = string.Concat(str.AsSpan(indexOfTheSpace + 1), " ", str.AsSpan(0, indexOfTheSpace - 1));
		// System.Console.WriteLine(fullName);

		// string company = "Microsoft Corporation";
		// bool startWith = company.StartsWith('M');
		// bool contain = company.Contains('C');
		// bool endWith = company.EndsWith('N');
		// System.Console.WriteLine(format: "Start with M: {0}, contain C: {1}, end with N: {2}", startWith, contain, endWith);


	}
}